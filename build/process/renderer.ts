import type { Template } from './template';
import { push } from './files';

function renderIndex(template: Template) {
  // ..
}

function renderPost(template: Template) {
  // ..
}

export function render(template: Template) {
  // ..
}

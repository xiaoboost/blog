name: Setup Environment
description: 准备依赖环境
runs:
  using: "composite"
  steps:
    - name: Create Env File
      shell: bash
      run: |
        touch .env
        echo 'ENV=GITHUB_CI' >> .env

    - name: Setup Node
      uses: actions/setup-node@v6
      with:
        node-version: '24'

    - name: Install Pnpm
      uses: pnpm/action-setup@v5
      with:
        run_install: false

    - name: Get Pnpm Store Directory
      id: cache-dir
      shell: bash
      run: |
        echo "store_path=$(pnpm store path --silent)" >> $GITHUB_OUTPUT
        echo "pnpm_version=$(pnpm --version)" >> $GITHUB_OUTPUT

    - name: Setup Pnpm Cache
      uses: actions/cache@v5
      with:
        path: ${{ steps.cache-dir.outputs.store_path }}
        key: ${{ runner.os }}-pnpm-${{ steps.cache-dir.outputs.pnpm_version }}-store-${{ hashFiles('**/pnpm-lock.yaml') }}
        restore-keys: |
          ${{ runner.os }}-pnpm-${{ steps.cache-dir.outputs.pnpm_version }}-store-

    - name: Install Dependencies
      shell: bash
      run: pnpm install --frozen-lockfile --prefer-offline

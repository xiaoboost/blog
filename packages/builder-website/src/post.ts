import { AssetData } from '@blog/utils';
import { PostData } from '@blog/posts';
import { Post as Render } from '@blog/template-post';

export function build(posts: PostData[]): AssetData[] {
  return [];
}

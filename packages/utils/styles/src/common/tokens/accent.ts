import { cssVar } from '../theme';

/** @see {@link AccentPrimary} */
export const AccentPrimaryToken = '--color-accent-primary';
/**
 * 主强调色
 *
 * @description 链接色等跨包场景。
 */
export const AccentPrimary = cssVar(AccentPrimaryToken);

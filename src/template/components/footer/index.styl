@import 'styles/variable.styl';

.page-footer {
    flex: 0 auto;
    font-family: font-default;
    font-weight: normal;
    margin: 40px 0 12px 0;
    width: 100%;
    color: BlackLighter;
    text-align: center;
    font-size: 12px;

    .page-footer-author {
        color: Black;
        transition: color .2s ease-out;

        &:hover {
            color: RedLight;
            cursor: pointer;
        }
    }
}
